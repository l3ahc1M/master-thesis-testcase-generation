{
  "difficulty": "Hard",
  "input": "Show the bank card contract lock requests having a user status code of '2'.",
  "output": {
    "method": "GET",
    "endpoint": "/BankCardContractLockRequests",
    "body": {
      "$filter": "UserStatusCode eq '2'"
    }
  }
}