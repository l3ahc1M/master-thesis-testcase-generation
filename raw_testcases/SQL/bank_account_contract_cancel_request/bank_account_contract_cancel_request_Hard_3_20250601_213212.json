{
  "difficulty": "Hard",
  "input": "Find the UUIDs of bank account contract cancel requests that have been cancelled and require a bank account settlement.",
  "output": {
    "sql": "SELECT UUID FROM BankAccountContractCancelRequest WHERE BankAccountSettlementRequiredIndicator = TRUE AND CancellationDate IS NOT NULL;"
  }
}