{
  "difficulty": "Medium",
  "input": "List the IDs of bank customer lock requests that are valid until the end of this month.",
  "output": {
    "sql": "SELECT ID FROM BankCustomerLockRequest WHERE ValidityEndDate <= LAST_DAY(CURRENT_DATE);"
  }
}