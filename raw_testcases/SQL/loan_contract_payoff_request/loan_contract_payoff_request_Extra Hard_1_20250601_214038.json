{
  "difficulty": "Extra Hard",
  "input": "Retrieve the loan contract payoff requests along with their corresponding loan contract UUIDs where the expected payment date is within the next 30 days.",
  "output": {
    "sql": "SELECT LoanContractUUID, ID FROM LoanContractPayoffRequest WHERE ExpectedPaymentDate BETWEEN CURDATE() AND DATE_ADD(CURDATE(), INTERVAL 30 DAY);"
  }
}